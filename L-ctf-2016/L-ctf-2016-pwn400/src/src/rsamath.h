#ifndef MYMATH_H
#define MYMATH_H

#include <cmath>

using namespace std;

bool is_prime(unsigned);
bool is_prime(unsigned);
bool are_coprime(unsigned , unsigned);
unsigned power(unsigned , unsigned);
unsigned powerwithmodule(unsigned , unsigned , unsigned );
#endif

